apiVersion: v1
kind: Service
metadata:
  name: {{ include "beer-catalogue.fullname" . }}
  labels:
    {{- include "beer-catalogue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "beer-catalogue.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP